<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.database.CocoCommunityMapper">
	<select id="showAllFriends" resultType="CocoFriend">
		SELECT FROM tb_friend WHERE = #{user_email} ORDER BY uploaded_at DESC
	</select>
	<select id="selectFriend" resultType="CocoFriend">
		SELECT FROM tb_friend WHERE = #{user_email}
	</select>
	<insert id="">
		INSERT
	</insert>
</mapper>